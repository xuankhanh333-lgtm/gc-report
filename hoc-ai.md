# Sổ AI thực chiến — khái niệm đã dạy trong chat

Mỗi dòng 1 khái niệm (mới nhất dưới cùng). Liếc trước khi dạy để không lặp.

| Ngày | Khái niệm | Tóm tắt 1 câu | Ngữ cảnh |
|---|---|---|---|
| 14/07/2026 | Calibrated probability | Model tốt không chỉ đoán đúng lớp mà xác suất phải "thật": pf 0.556 nghĩa là nhóm setup giống vậy thắng ~55.6% — nhờ đó nhân ra EV được | TAKE LONG pf 0.556 trước CPI → TP; ngưỡng TAKE≥0.40 của pooldir chính là cắt theo EV |
| 14/07/2026 | Pre-registration vs hindsight bias | Dự đoán phải GHI TRƯỚC khi có kết quả rồi mới chấm — nhìn lại sau khi biết đáp án thì cái gì cũng "thấy trước được", win-rate ảo | Checklist CPI→VN 4 ô đăng ký tối 14/07 chấm sáng 15/07; cùng luật với "test model mới phải đăng ký dự đoán trước" của vnpool D1 vs H4 |
| 14/07/2026 | AUC ngược (inverted classifier) | AUC < 0.5 không phải "không có tín hiệu" mà là tín hiệu NGƯỢC — model đảo dấu sẽ được AUC = 1−x; nhưng đừng vội đảo: phải tìm nguyên nhân (feature chết, leak, regime) trước | gc-llm RT AUC 0.29–0.35 (đảo ra 0.65–0.71) trong lúc DXY/US10Y đóng băng 99.88/4.483 hơn 1 tháng — nghi feature chết chứ chưa chắc edge ngược |
| 14/07/2026 | Undersampling (lấy mẫu thưa hơn tín hiệu) | Điều kiện biến đổi nhanh (M5) mà chỉ kiểm tra ở nhịp chậm (H4 close, 6 lần/ngày) thì sự kiện xảy ra giữa 2 lần lấy mẫu bị mất sạch — sửa bằng tăng tần suất check, không phải nới ngưỡng | gc-llm entry: 3/3 lần M5 score≥2 đều bị block "price_outside_zone_hard" vì tới H4 close giá đã rời zone; khi giá còn trong zone thì score=0-1 → 0 lệnh khớp từ 12/06 |
| 14/07/2026 | Ensemble disagreement = uncertainty | Hai model cùng hệ nhưng nhìn 2 khung khác nhau mà ngược phe thì độ lệch giữa chúng chính là thước đo bất định (epistemic uncertainty) — tin cậy chỉ khi các view đồng thuận, ngược nhau = giảm size/đứng ngoài chứ không chọn phe theo cảm tính | NVL 14/07: nhánh H4 TAKE SHORT pf 0.478 (net conf 0.77) vs nhánh D1 TAKE LONG pf 0.498 (net conf 0.83) — 2 sổ đang chạy song song A/B |
| 14/07/2026 | Proxy variable (biến đại diện) | Khi chuỗi mục tiêu không lấy được trực tiếp thì dùng biến tương quan cao thay thế — chất lượng proxy đặt TRẦN cho model, phải đo corr proxy↔target trước khi tin | FRED đã gỡ giá vàng LBMA (bản quyền) → trên FRED chỉ còn proxy: IQ12260 (chỉ số giá XK vàng phi tiền tệ, monthly) + GVZCLS (vol GLD 26.93); macro-gold vì thế lấy giá GC từ TradingView/Databento, FRED chỉ cấp 14 feature vĩ mô (DFII10 2.32%, T10YIE 2.26%, DTWEXBGS 120.5) |
| 14/07/2026 | Silent data truncation (nguồn tự cắt lịch sử) | Nguồn dữ liệu đổi chính sách lưu trữ → chuỗi tự ngắn lại KHÔNG báo lỗi, pipeline retrain trên lịch sử cụt mà không ai hay — phải guard bằng check observation_start | Phát hiện khi gộp 2 phiên FRED 14/07: BAMLH0A0HYM2 (HY spread) giờ chỉ còn obs từ 2023-07-17 — FRED cắt các chuỗi ICE BofA về cửa sổ 3 năm từ 04/2026, trong khi trước đó có từ 1996; macro-gold nếu thêm HY spread phải archive lịch sử dài trước khi bị cắt |
| 14/07/2026 | Latent variable (biến ẩn suy từ model) | Đại lượng không quan sát trực tiếp được (term premium, regime, uncertainty) phải TRÍCH từ model cấu trúc (Kim-Wright 3-factor arbitrage-free) — dùng nó làm feature tức là kế thừa cả assumption + độ trễ của model gốc, khác hẳn feature đo trực tiếp từ giá | FRED THREEFYTP10: term premium 10Y = 0.73%% (02/07/2026), series daily từ 1990 nhưng trễ ~1-2 tuần so với DFII10 realtime T-1 — hợp làm feature slow-moving regime, không hợp signal daily |
| 15/07/2026 | Multicollinearity (đa cộng tuyến) | Hai-ba feature là MỘT đẳng thức của nhau thì model chia importance lung tung giữa chúng, thêm feature "mới" kiểu này không thêm thông tin — chỉ thêm nhiễu; chọn 2 trong 3 là đủ | DFII10 = DGS10 − T10YIE (2.32 ≈ 4.56 − 2.26, lệch nhỏ do khác ngày obs) — FWD của macro-gold đã có bei_r40 + r10_r20 nên ĐỪNG thêm tips/DFII10 vào FWD (BWD dùng tips riêng thì hợp lệ); cùng lý do candidate REAINTRATREARAT10Y phải thế chỗ chứ không xếp cạnh |
| 15/07/2026 | Dead-zone theo σ (noise gate) | Muốn phân loại tăng/giảm/đi-ngang thì đừng lấy sign(Δ) trần — mọi nhiễu nhỏ sẽ thành tín hiệu; đặt vùng chết tỉ lệ với vol CHÍNH series đó (|Δ| ≤ 0.5σ√W → 0), series càng ồn ngưỡng càng cao | FRED scorecard 15/07: KCROROG nhảy +0.898 vẫn chấm "—" vì RORO daily vol rất cao (0.9 < 0.5σ√5), trong khi DFII10 +0.12 đủ ăn ▲ vì TIPS yield σ ngày chỉ ~0.03-0.05 — cùng một luật, ngưỡng tự co giãn theo từng series |
| 15/07/2026 | Oracle test / golden master | Trước khi cho bản tái tạo thay bản gốc, bắt nó chạy lại và SO OUTPUT với kết quả đã ghi của bản gốc (oracle) — khớp 100% mới tin; lệch lẻ = data mới, lệch hàng loạt = bug | fred scorecard v2 (laptop tái tạo từ HTML vì không có source): gate "dirs khớp history line cuối", lệch ≥5 series thì abort không push; desktop verify 37/37 mismatch NONE; nhưng oracle chỉ phủ series từng có dir≠0 — 12 dấu guessed dir=0 lọt lưới, đối chiếu source gốc mới lòi 2 dấu sai (KCROROG, USEPUINDXD) |
| 19/07/2026 | Right-censoring (nhãn chưa chín) | Sự kiện ở mép phải chưa đủ cửa sổ quan sát thì kết cục CHƯA BIẾT (censored) — không được đem train (thành nhiễu) cũng không được chấm win-rate (thiên vị kèo ngắn); chỉ hiển thị + chờ chín rồi backfill | P_cascade htf-liq3: mỗi pierce chờ tối đa 40 nến H4 (~1.5 tuần) để chốt COLLAPSE/SWEEP/CHOP; chưa đủ 40 bar → label OPEN, clean_lib loại khỏi tập train (`keep = labels != OPEN`), trend page ghi "đang mở"; gen_trend giữ P bản LIVE vì recompute lúc nhãn chín cho P khác |
| 19/07/2026 | Point-in-time data (as-of, chống repaint) | Muốn biết "model nói gì LÚC ĐÓ" phải đóng băng snapshot ngay lúc chạy — swing/pool chỉ xác nhận khi có thêm nến bên phải nên chạy lại sau trên cùng bar ra số khác; đánh giá model bằng số tính lại = look-ahead trá hình | gen_trend.py: dot P_cascade vòng cam giữ P từ pooldir_history.jsonl (point-in-time), recompute chỉ backfill + cấp "kết cục thật", tooltip tách 'live' vs 'hồi tố'; cùng triết lý lớp live overlay (đường đứt + vòng rỗng) ở P_follow/p_up; nhãn kết cục đổi sau là chấm công, không phải repaint tín hiệu |
| 20/07/2026 | Low-beta anomaly (Betting Against Beta) | Trái CAPM, cổ phiếu beta THẤP lịch sử cho lợi nhuận điều-chỉnh-rủi-ro tốt hơn beta cao (nhà đầu tư bị ràng buộc đòn bẩy trả giá quá đắt cho beta) — nên composite chất lượng thường PHẠT beta cao chứ không thưởng | vnpool rk_score thêm −0.08·z(beta) kẹp ±1.5 (beta_1_year TV, 68/69 mã, dải −0.34→2.12): SSI β 1.15 tụt hạng Gom đáy 11→14, PVD β thấp lên 14→11, top 3 BSR/MWG/HPG không đổi; đổi khẩu vị chỉ cần lật dấu W.beta trong CFG |
