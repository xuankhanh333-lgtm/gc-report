# Deploy GitHub Pages qua Actions (build_type=workflow) — thay pipeline legacy bi ket 06/08/2026
# (legacy builder loi "Page build failed" tu 17:35, build treo vo han sau khi reset).
# Bot push ~15'/lan, co khi cap cach 14s -> KHONG cancel-in-progress: huy giua buoc deploy-pages
# de lai deployment ket "in progress" tren server (dung benh da lam Pages chet 17:35). Xep hang:
# GitHub tu gop cac run cho trong cung group, chi giu ban moi nhat.
name: Deploy Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
concurrency:
  group: pages
  cancel-in-progress: false
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
