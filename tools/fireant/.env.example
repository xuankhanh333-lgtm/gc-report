# Sao chép file này thành ".env" rồi dán token FireAnt vào.
# File ".env" đã được .gitignore — sẽ KHÔNG bị commit/push.
# Lấy token: đăng nhập fireant.vn -> DevTools -> Network -> request tới
# restv2.fireant.vn -> copy phần sau "Bearer " trong header Authorization.
FIREANT_TOKEN=
